[package]
name = "openra-data"
description = "File format parsers for .orarep, .oramap, SHP sprites, and palettes"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
bitflags = "2"
zip = { version = "2", default-features = false, features = ["deflate"] }
