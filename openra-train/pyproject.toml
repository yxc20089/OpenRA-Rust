[build-system]
requires = ["maturin>=1.0"]
build-backend = "maturin"

[project]
name = "openra_train"
version = "0.1.0"
requires-python = ">=3.9"

[tool.maturin]
bindings = "pyo3"
features = ["python"]
